package com.giyeok.jparser.study.visualize

import com.giyeok.jparser.Inputs
import com.giyeok.jparser.study.parsing.AnalysisItem
import com.giyeok.jparser.study.parsing.PredictionStackContext
import com.giyeok.jparser.study.parsing.PredictionStackItem

class PredictionStackContextVisualize {
    // html element로 나오게 하고 싶음
    def visualizeContext(context: PredictionStackContext): Unit = {
        ???
    }

    def visualizePredictionStackItem(item: PredictionStackItem): Unit = {
        ???
    }

    def visualizeAnalysisTem(item: AnalysisItem): Unit = {
        ???
    }

    def visualizeInput(input: Inputs.Input): Unit = {
        ???
    }
}
