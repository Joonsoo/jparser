package com.giyeok.jparser.study.grammarmodification

import com.giyeok.jparser.study.ContextFreeGrammar
import com.giyeok.jparser.study.ContextFreeGrammarModification

class EpsilonAndUnitRulesElimination {
    def eliminateEmptyRules(grammar: ContextFreeGrammar): (ContextFreeGrammar, ContextFreeGrammarModification) = {
        ???
    }

    def eliminateUnitRules(grammar: ContextFreeGrammar): (ContextFreeGrammar, ContextFreeGrammarModification) = {
        ???
    }
}
